if not game:IsLoaded() then
    game.Loaded:Wait()
end
task.wait(5)

local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local TextChatService = game:GetService("TextChatService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer or Players.PlayerAdded:Wait()

if _G.DeltaServerHopActive then return end
_G.DeltaServerHopActive = true

local MESSAGE = "Azerbaycan sohbet oyunu yaz gir"
local MAX_SPAM_COUNT = 15
local DELAY = 0.2 
local currentSpamCount = 0
local jumped = false

local function getAbsoluteNewServer()
    local success, result = pcall(function()
        -- Oyunun aktif 100 sunucusunu listeliyoruz
        local url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Desc&limit=100"
        local raw = game:HttpGet(url)
        local servers = HttpService:JSONDecode(raw)
        local validServers = {}
        
        for _, server in pairs(servers.data) do
            -- Sunucuda yer olacak ve KESİNLİKLE şu anki sunucumuzun ID'sine eşit olmayacak
            if server.playing < server.maxPlayers and server.id ~= game.JobId and server.playing > 1 then
                table.insert(validServers, server.id)
            end
        end
        
        -- Listeden tamamen rastgele bir sunucu seçerek çakışmayı önlüyoruz
        if #validServers > 0 then
            return validServers[math.random(1, #validServers)]
        end
    end)
    return success and result or nil
end

local function serverHop()
    if jumped then return end
    jumped = true
    _G.DeltaServerHopActive = nil
    
    -- Verilerin hatasız kaydedilmesi için 6 saniye tam emniyet beklemesi
    task.wait(6)
    
    local targetServerId = getAbsoluteNewServer()
    
    task.spawn(function()
        while true do
            pcall(function()
                if targetServerId then
                    -- Doğrudan o farklı sunucunun içine hedef gözeterek ışınlar (Kick yemeyi engeller)
                    TeleportService:TeleportToPlaceInstance(game.PlaceId, targetServerId, LocalPlayer)
                else
                    TeleportService:Teleport(game.PlaceId, LocalPlayer)
                end
            end)
            task.wait(7)
        end
    end)
end

local function sendMessage(text)
    if jumped then return end
    pcall(function()
        if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
            local textChannel = TextChatService:FindFirstChild("TextChannels") and TextChatService.TextChannels:FindFirstChild("RBXGeneral")
            if textChannel then
                textChannel:SendAsync(text)
                currentSpamCount = currentSpamCount + 1
            end
        else
            local remote = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents") and ReplicatedStorage.DefaultChatSystemChatEvents:FindFirstChild("SayMessageRequest")
            if remote then
                remote:FireServer(text, "All")
                currentSpamCount = currentSpamCount + 1
            end
        end
    end)
end

pcall(function()
    local playerGui = LocalPlayer:WaitForChild("PlayerGui", 25)
    playerGui.DescendantAdded:Connect(function(descendant)
        if jumped then return end
        if descendant:IsA("TextLabel") then
            task.wait(0.05)
            local txt = string.lower(descendant.Text)
            if string.find(txt, "beklemelisin") or string.find(txt, "before sending") or string.find(txt, "#") then
                serverHop()
            end
        end
    end)
end)

task.spawn(function()
    while not jumped do
        if currentSpamCount >= MAX_SPAM_COUNT then
            serverHop()
            break
        end
        sendMessage(MESSAGE)
        task.wait(DELAY)
    end
end)
